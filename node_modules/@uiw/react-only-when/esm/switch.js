import { Children } from 'react';
export var Switch = _ref => {
  var _ref2, _matchChild;
  var {
    children
  } = _ref;
  var matchChild = null;
  var defaultCase = null;
  Children.forEach(Children.toArray(children), child => {
    if (!matchChild && child.type === Case) {
      var {
        condition
      } = child.props;
      var conditionIsTrue = Boolean(condition);
      if (conditionIsTrue) {
        matchChild = child;
      }
    } else if (!defaultCase && child.type === Default) {
      defaultCase = child;
    }
  });
  return (_ref2 = (_matchChild = matchChild) != null ? _matchChild : defaultCase) != null ? _ref2 : null;
};
export var Case = _ref3 => {
  var {
    children
  } = _ref3;
  return children;
};
export var Default = _ref4 => {
  var {
    children
  } = _ref4;
  return children;
};